<UserControl
    x:Class="MinskTrans.Universal.ShowStopView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MinskTrans.Universal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:modelview="using:MinskTrans.DesctopClient.Modelview"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    d:DataContext="{d:DesignInstance modelview:GroupStopsModelView}">
    <UserControl.Resources>
        <DataTemplate x:Key="DataTemplate1">
            <Grid x:Name="Grid">
                <Grid.ColumnDefinitions>

                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition  Width="Auto"/>
                    <ColumnDefinition/>
                    <ColumnDefinition  Width="Auto"/>


                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>

                </Grid.RowDefinitions>
                <Image Grid.RowSpan="3" 
					Source="{Binding Key.Transport, Converter={StaticResource TransportToImageConverter}}" Stretch="Uniform" HorizontalAlignment="Left" VerticalAlignment="Top"
					   Height="{Binding ActualHeight, ElementName=StackPanel}"/>
                <StackPanel Grid.Column="1" HorizontalAlignment="Left">
                    <StackPanel x:Name="StackPanel">
                        <TextBlock Grid.Column="1" 
					Text="{Binding Key.RouteNum}"
					FontSize="25"/>
                        <TextBlock Grid.Column="1" 
					Grid.Row="1"
					Text="{Binding Key.StartStop.Name}"/>
                        <TextBlock Grid.Column="1" 
									Grid.Row="2"
									Text="{Binding Key.DestinationStop.Name}" FontSize="14"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="3" HorizontalAlignment="Right">
                    <TextBlock Grid.Column="2" 
					Text="{Binding Value, Converter={StaticResource MinsToTimeConverter}}"
					FontSize="25" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Column="2" 
					Grid.Row="1"
					Text="{Binding Value, Converter={StaticResource MinsLeftConverter}}"
					FontSize="14" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid x:Name="ShowStop" 
					  Visibility="Collapsed"
					  Grid.Row="1"
					  Grid.Column="0"
					  Grid.ColumnSpan="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding FilteredSelectedStop.Name, UpdateSourceTrigger=PropertyChanged}"/>
        <ListBox x:Name="tesstListview"
				    HorizontalContentAlignment="Stretch"
					Grid.Row="1" 
					ItemsSource="{Binding TimeSchedule, UpdateSourceTrigger=PropertyChanged}"
					ItemTemplate="{StaticResource DataTemplate1}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</UserControl>
