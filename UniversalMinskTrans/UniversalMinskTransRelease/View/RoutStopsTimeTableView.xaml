<UserControl
    x:Class="UniversalMinskTransRelease.View.RoutStopsTimeTableView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UniversalMinskTransRelease.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:NumberConverter.WinRTXamlToolkit.Controls"
    xmlns:modelView="using:MinskTrans.Universal.ModelView"
    xmlns:converters="using:MinskTrans.Universal.Converters"
    xmlns:expanderControl="using:ExpanderControl"
    xmlns:universal="using:MinskTrans.Universal"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance modelView:RouteStopsTimeTableList}"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <converters:TransportFormatedStringConverter x:Key="DateToString" />

        <expanderControl:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <converters:WeekdaysToStrConverter x:Key="WeekdaysToStrConverter" />

        <converters:IntToVisibleConverter x:Key="IntToVisibleConverter" />
        <converters:RoutsToNumStrConverter x:Key="RoutsToNumStrConverter" />
        <converters:StopsToStr x:Key="StopsToStr" />
        <converters:TransportToImageConverter x:Key="TransportToImageConverter" />
        <universal:MinsLeftConverter x:Key="MinsLeftConverter" />
        <universal:MinsToTimeConverter x:Key="MinsToTimeConverter" />
    </UserControl.Resources>
    <ListView 
                                      ItemContainerStyle="{StaticResource FullListViewItem}"
                                      ItemsSource="{Binding UpdateSourceTrigger=PropertyChanged}">

        <ListView.ItemTemplate>
            <DataTemplate>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <ToggleButton x:Name="ToggleButton"
                                                          HorizontalAlignment="Stretch"
                                                          Content="{Binding Stop.Name}"
                                                          FontSize="17" 
                                  
                                                            Style="{ThemeResource ToggleButtonStyle1}" >
                    </ToggleButton>
                    <!--<AppBarButton Visibility="{Binding IsChecked, ElementName=ToggleButton, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Right" Icon="Calendar" ></AppBarButton>-->
                    
                    <ListView Grid.Row="1"
                                                      ItemsSource="{Binding TimeTable}"
                                                      Visibility="{Binding IsChecked,
                                                                           Converter={StaticResource BooleanToVisibilityConverter},
                                                                           ElementName=ToggleButton}">
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <controls:WrapPanel />
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="5,0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock FontSize="15" Text="{Binding DaysStr}" />
                                    <TextBlock Grid.Row="1"
                                                                       FontSize="13"
                                                                       Text="{Binding ToStrTable}" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
